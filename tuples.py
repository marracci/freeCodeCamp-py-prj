
# Tuples - data structure container
coordinates = (46, 121)                 # Tuples can't be modified, aka immutable. use () to declare
print(coordinates[1])                   # indexed starting at 0
coordinates2 = [(46,121), (40,105), (32, 118)]   # you can create a list of Tuples, but can;t modify the tuples within
print(coordinates2)


